#veriable lets you store data for later use in the code. can refer to that data by the variable name
#data in the variable can be changed
#make your code readable, use veriable names that make sense
#numbers cannot be in the begining of the variable
#dont use the name of functions such as print or input as a variable name
#use underscore to have variable that consists of multiple words
name = "Jack"
print(name)

name = "Ron"
print(name)

name = input ("What is your name ? ")
length = len(name)
print (length)